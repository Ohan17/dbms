import { makeStyles } from "@material-ui/core/styles";
export default makeStyles((theme) => ({
  root: {
    paddingTop: 60,
  },
  search:{
    color:"blue",
    background:"blue",
  },
  bar:{
    
    marginTop:10,
    marginBottom:20,
  }
}));
